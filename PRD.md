# 우리 동네 카페 지도 서비스 PRD

## 목차

1. [개요](#1-개요)
2. [용어 정의](#2-용어-정의)
3. [사용자 및 권한](#3-사용자-및-권한)
4. [핵심 기능 요구사항](#4-핵심-기능-요구사항)
5. [사용자 시나리오](#5-사용자-시나리오)
6. [기술 스택 및 아키텍처](#6-기술-스택-및-아키텍처)
7. [데이터 모델 (Supabase)](#7-데이터-모델-supabase)
8. [RLS(행 수준 보안) 정책](#8-rls행-수준-보안-정책)
9. [환경변수 및 키 관리](#9-환경변수-및-키-관리)
10. [화면 구성](#10-화면-구성)
11. [비기능 요구사항](#11-비기능-요구사항)
12. [범위 제외 (Out of Scope)](#12-범위-제외-out-of-scope)
13. [미확정 항목 — 확인 필요](#13-미확정-항목--확인-필요)

---

## 1. 개요

### 1.1 배경
동네 카페 목록을 엑셀로 정리해왔지만, 위치를 한눈에 보거나 방문 여부·소감을 기록/공유할 방법이 없었음. 엑셀 데이터를 지도 위에 시각화하고, 개인별 방문 기록을 남길 수 있는 웹 서비스가 필요함.

### 1.2 목적
- 관리자가 업로드한 엑셀 카페 목록을 카카오맵 위에 자동으로 마커 표시
- 로그인한 사용자가 카페별 방문 여부와 한줄 소감을 개인 기록으로 저장
- 방문한 카페만 모아보는 개인 목록 제공

### 1.3 대상 서비스 형태
- 웹 서비스 (Vercel 배포)
- 카페 목록은 **전체 공용 데이터** (관리자가 업로드, 모든 사용자가 동일한 지도를 봄)
- 방문 체크/소감은 **로그인 사용자 개인별 데이터**

---

## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| 카페 목록 | 관리자가 엑셀로 업로드하는 이름/주소/카테고리 데이터. 전체 공용. |
| 지오코딩 | 주소 문자열을 위도/경도 좌표로 변환하는 과정 (카카오맵 SDK 이용) |
| 방문 기록 | 로그인 사용자가 특정 카페(이름+주소)에 대해 남기는 방문 여부 + 한줄 소감 |
| 동일 장소 | "카페 이름 + 주소"가 동일한 경우를 같은 장소로 간주 |
| 관리자 | 엑셀 업로드 권한을 가진 사용자 (일반 회원과 구분) |

---

## 3. 사용자 및 권한

| 구분 | 지도(마커) 열람 | 엑셀 업로드 | 방문체크/소감 입력 | 내 방문목록 조회 |
|---|---|---|---|---|
| 비회원 | O | X | X | X |
| 일반 회원 (로그인) | O | X | O (본인 것만) | O (본인 것만) |
| 관리자 | O | O | O | O |

- 지도 열람은 비회원에게도 공개 (읽기 전용)
- 방문체크/소감 입력 및 조회는 로그인 필요
- 엑셀 업로드는 관리자 전용 (판별 방식은 [13장](#13-미확정-항목--확인-필요) 참고)

---

## 4. 핵심 기능 요구사항

### 4.1 엑셀 파일 업로드 및 카페 목록 읽기
- 관리자가 `.xlsx` 파일을 업로드
- 필수 컬럼: **이름, 주소, 카테고리**
- 업로드 즉시 파싱 → 카페 목록 테이블 갱신 (정책: **덮어쓰기**, [7장](#7-데이터-모델-supabase) 참고)
- 필수 컬럼이 없거나 행이 비어있는 경우 해당 행은 건너뛰고 업로드 결과 화면에 "실패/스킵 행" 목록으로 안내

### 4.2 주소 → 좌표 변환 및 마커 표시
- 카카오맵 JavaScript SDK의 지오코딩(Geocoder) 기능을 프론트엔드에서 직접 호출하여 주소를 좌표로 변환
- 변환 성공 시: 해당 좌표에 마커 표시, 카테고리별 마커 아이콘/색상 구분(선택)
- 변환 실패 시(주소 매칭 안 됨): 지도에는 표시하지 않고, 화면 내 "좌표 변환 실패 목록" 영역에 카페명/주소를 별도로 안내
- 재시도 버튼 등 수동 보정 기능은 MVP 범위에서 제외 (필요 시 관리자가 엑셀 주소를 수정 후 재업로드)

### 4.3 마커 클릭 → 방문 여부 체크 + 한줄 소감
- 지도 위 마커 클릭 시 카페 정보 팝업(이름/주소/카테고리) 표시
- 로그인 사용자: "방문함/안함" 체크박스(토글) + 한줄 소감 입력창 노출
- 비로그인 사용자: 카페 정보만 조회 가능, 체크/소감 입력창 대신 로그인 유도 문구 표시
- 저장 시 Supabase에 upsert (동일 사용자 + 동일 카페 조합은 1건만 유지, [8장](#8-rls행-수준-보안-정책) 참고)

### 4.4 회원가입/로그인 및 소감 영속성
- Supabase Auth 기반 이메일 + 비밀번호 회원가입/로그인
- 로그인한 사용자가 같은 카페 마커를 다시 클릭하면, 이전에 저장한 체크 상태와 소감이 그대로 불러와져 표시됨
- 소감 수정 시 기존 레코드를 업데이트 (새 레코드 생성 아님)

### 4.5 방문 체크한 카페 목록 화면
- 로그인 사용자 전용 화면
- 본인이 "방문함"으로 체크한 카페만 리스트로 표시 (이름, 주소, 카테고리, 소감, 방문 체크 시각)
- 엑셀 업로드 여부와 무관하게 항상 로그인 사용자 개인 데이터 기준으로 조회
- 목록에서 각 항목 클릭 시 지도상 해당 위치로 이동(선택 기능, [13장](#13-미확정-항목--확인-필요) 참고)

### 4.6 방문한 카페 마커 색상 구분
- 로그인 사용자가 "방문함"으로 체크한 카페는 지도 마커 색을 미방문 카페와 다르게 표시
- 방문 여부는 본인의 `visits` 데이터 기준으로 판단 (다른 사용자의 방문 체크는 마커 색에 영향 없음)
- 비로그인 상태에서는 방문 여부를 알 수 없으므로 모든 마커를 기본 색상으로 표시

---

## 5. 사용자 시나리오

**시나리오 A — 관리자**
1. 관리자 로그인 → 엑셀 업로드 화면 진입
2. 엑셀 파일 선택 후 업로드
3. 파싱 결과(성공 N건 / 실패 M건) 확인
4. 지도 화면에서 마커 반영 확인

**시나리오 B — 비회원 방문자**
1. 서비스 접속 → 지도 화면에서 마커 확인
2. 마커 클릭 → 카페 정보만 열람
3. 체크/소감 입력 시도 → 로그인 페이지로 유도

**시나리오 C — 일반 회원**
1. 회원가입/로그인
2. 지도에서 마커 클릭 → 방문함 체크 + 소감 입력 → 저장
3. 이후 재접속 후 같은 마커 클릭 → 이전 체크/소감이 그대로 보임
4. "내가 방문한 카페" 목록 화면에서 본인 기록만 확인

---

## 6. 기술 스택 및 아키텍처

| 영역 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | Next.js (React) | Vercel 배포 전제 |
| 지도/지오코딩 | 카카오맵 JavaScript SDK (services 라이브러리) | 프론트에서 직접 호출, 별도 서버 프록시 없음 |
| 엑셀 파싱 | 클라이언트 사이드 파싱 라이브러리 (예: SheetJS/xlsx) | 브라우저에서 읽어 Supabase로 전송 |
| 인증 | Supabase Auth (이메일+비밀번호) | |
| 데이터베이스 | Supabase Database (PostgreSQL) + RLS | |
| 배포 | Vercel | 환경변수는 Vercel 프로젝트 설정에서 관리 |

**아키텍처 개요**
```
[사용자 브라우저]
   ├─ 카카오맵 JS SDK ── 카카오 지도/지오코딩 API (직접 호출)
   └─ Supabase JS Client ── Supabase Auth / Database (RLS 적용)
[Vercel]
   └─ Next.js 정적/서버 자원 호스팅 + 환경변수 주입
```
- 별도의 커스텀 백엔드 서버 없이 Supabase를 BaaS로 사용
- 관리자의 엑셀 업로드도 Supabase 클라이언트를 통해 직접 insert (RLS로 관리자만 허용)

---

## 7. 데이터 모델 (Supabase)

### 7.1 `cafes` (카페 목록 — 공용 데이터)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| name | text | 카페 이름 |
| address | text | 주소 (엑셀 원본 텍스트) |
| category | text | 카테고리 |
| lat | double precision, nullable | 지오코딩 성공 시 위도 |
| lng | double precision, nullable | 지오코딩 성공 시 경도 |
| geocode_status | text | `success` / `failed` |
| created_at | timestamptz | |
| updated_at | timestamptz | |

- 엑셀 재업로드 시: 기존 행 전체 삭제 후 새 데이터로 교체 (덮어쓰기)
- `visits`는 `cafes.id`가 아닌 **name + address 텍스트 기준**으로 연결하여, 재업로드로 id가 바뀌어도 개인 기록이 유지되도록 함

### 7.2 `visits` (방문 체크 + 소감 — 개인 데이터)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | |
| user_id | uuid (FK → auth.users.id) | |
| cafe_name | text | 카페 이름 (매칭 키) |
| cafe_address | text | 카페 주소 (매칭 키) |
| visited | boolean | 방문 여부 |
| review | text, nullable | 한줄 소감 |
| created_at | timestamptz | |
| updated_at | timestamptz | |

- **UNIQUE 제약: (user_id, cafe_name, cafe_address)** → 동일 사용자가 동일 장소에 남긴 기록은 항상 1건만 유지
- 저장 로직: `INSERT ... ON CONFLICT (user_id, cafe_name, cafe_address) DO UPDATE`

### 7.3 `profiles` (역할 구분용, 관리자 판별)
- 관리자 판별 방식이 [13장](#13-미확정-항목--확인-필요)에서 확정되면 스키마 확정 (예: `is_admin boolean`)

---

## 8. RLS(행 수준 보안) 정책

### `cafes`
- **SELECT**: 모든 사용자(비회원 포함) 허용
- **INSERT / UPDATE / DELETE**: 관리자만 허용

### `visits`
- **SELECT**: `auth.uid() = user_id` 인 행만 허용 (본인 것만 조회)
- **INSERT**: `auth.uid() = user_id` 인 경우만 허용
- **UPDATE**: `auth.uid() = user_id` 인 행만 허용
- **DELETE**: `auth.uid() = user_id` 인 행만 허용 (필요 시)

> 위 정책으로 "로그인 후 본인 소감 저장, 재방문 시 본인 것만 그대로 노출, 방문 목록도 본인 것만 조회"가 DB 레벨에서 보장됨.

---

## 9. 환경변수 및 키 관리

| 변수명 | 위치 | 용도 |
|---|---|---|
| `NEXT_PUBLIC_KAKAO_MAP_KEY` | `.env` (로컬) / Vercel 환경변수 | 카카오맵 JS SDK 키 (클라이언트 노출, 카카오 개발자센터에서 도메인 제한 설정 필요) |
| `NEXT_PUBLIC_SUPABASE_URL` | `.env` / Vercel | Supabase 프로젝트 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `.env` / Vercel | Supabase anon key (RLS로 접근 제어되므로 클라이언트 노출 가능) |

- 모든 키는 코드에 하드코딩하지 않고 `.env` / Vercel 프로젝트 설정을 통해서만 주입
- `.env` 파일은 `.gitignore`에 포함하여 저장소에 커밋되지 않도록 함
- 카카오 개발자센터에서 서비스 도메인(로컬/Vercel 배포 도메인)을 플랫폼 등록해야 함

---

## 10. 화면 구성

1. **지도 화면 (메인)**: 카카오맵 + 카페 마커, 좌표 변환 실패 목록 영역, 로그인 상태 표시
2. **마커 팝업**: 카페 정보 + (로그인 시) 방문 체크 토글 + 한줄 소감 입력
3. **로그인 / 회원가입 화면**: 이메일+비밀번호
4. **엑셀 업로드 화면 (관리자 전용)**: 파일 선택, 업로드 결과(성공/실패 건수, 실패 사유)
5. **내가 방문한 카페 목록 화면**: 로그인 사용자 개인 방문 리스트

---

## 11. 비기능 요구사항

- 엑셀 대량 업로드 시 카카오 지오코딩 API 호출 제한(초당 요청 수)을 고려하여 순차/배치 처리 필요
- 지오코딩 처리 중 진행 상태(예: "120/300건 변환 중") 표시 권장
- 비회원도 지도 조회가 가능해야 하므로 초기 로딩 시 인증 여부와 무관하게 `cafes` 데이터를 먼저 로드
- 개인정보(이메일 외 소감 텍스트)는 사용자 본인 외 노출되지 않아야 함 (RLS로 보장)

---

## 12. 범위 제외 (Out of Scope)

- 카페 정보 직접 등록/수정 UI (엑셀 업로드로만 관리)
- 소셜 로그인(카카오 로그인 등)
- 실패 주소에 대한 수동 좌표 입력/보정 UI
- 카테고리 필터, 검색, 정렬 등 지도 부가 기능
- 리뷰 공유/타인 소감 열람, 좋아요, 댓글 등 소셜 기능
- 모바일 앱(네이티브)

---

## 13. 미확정 항목 — 확인 필요

코드 작성 전에 아래 항목은 결정이 필요합니다.

1. **관리자 판별 방식**: 특정 이메일을 하드코딩/allowlist로 관리할지, `profiles.is_admin` 같은 DB 플래그로 관리할지?
2. **엑셀 템플릿 규칙**: 컬럼명이 정확히 "이름/주소/카테고리"로 고정인지, 헤더 위치나 시트 이름 규칙이 있는지?
3. **업로드 파일 크기/행 수 제한**: 최대 몇 건까지 지원할지 (지오코딩 처리 시간과 직결)
4. **카테고리 값의 종류**: 카테고리가 자유 텍스트인지, 정해진 목록(예: 디저트/스터디카페/로스터리 등)인지?
5. **마커 디자인**: 카테고리별로 마커 색상/아이콘을 구분할지, 단일 마커로 통일할지?
6. **방문 목록 화면에서 지도 연동 여부**: 목록 항목 클릭 시 지도 이동/포커스 기능이 필요한지?
7. **비밀번호 재설정/이메일 인증 메일**: Supabase 기본 이메일 인증 플로우를 그대로 사용할지, 커스텀 문구/도메인이 필요한지?

---

*문서 버전: v1.0 (초안) — 작성일 2026-07-27*
